class Solution {
    public int jump(int[] nums) {
      int minstep = nums[0];
      for(int i=0; i<nums.length-1;i++) {
          minstep = Math.min(nums[0] , nums[i+1]-nums[i]);
      } 
      return minstep;
    }
}
